FROM python:3.11-slim

# Create a non-privileged user
RUN useradd -m -s /bin/bash sandbox

USER sandbox
WORKDIR /home/sandbox

# Pre-install some common libraries if needed
# RUN pip install numpy pandas --no-cache-dir

CMD ["python3"]
